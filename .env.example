# BinnAgent Environment Configuration
# Copy this file to .env and adjust values for your setup.

# Database
BINN_DATABASE_URL=postgresql+asyncpg://binn:binn@localhost:5432/binn_agent

# Ollama (local LLM)
BINN_OLLAMA_BASE_URL=http://localhost:11434
BINN_OLLAMA_CHAT_MODEL=gemma4:e2b
BINN_OLLAMA_UTILITY_MODEL=gemma4:e2b
BINN_OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest

# Model provider switch
# LongCat is the default chat and utility provider.
# RAG embeddings remain isolated on Ollama for now.
BINN_MODEL_PROVIDER=longcat

# DeepSeek API (OpenAI-compatible)
BINN_DEEPSEEK_BASE_URL=https://api.deepseek.com
BINN_DEEPSEEK_API_KEY=
BINN_DEEPSEEK_CHAT_MODEL=deepseek-v4-flash
BINN_DEEPSEEK_UTILITY_MODEL=deepseek-v4-flash

# LongCat API (OpenAI-compatible)
BINN_LONGCAT_BASE_URL=https://api.longcat.chat/openai
BINN_LONGCAT_API_KEY=
BINN_LONGCAT_CHAT_MODEL=LongCat-2.0
BINN_LONGCAT_UTILITY_MODEL=LongCat-2.0

# Chat context and continuation
BINN_CHAT_MAX_TOKENS=2048
BINN_CHAT_HISTORY_LIMIT=12
BINN_CHAT_AUTO_CONTINUE_LIMIT=2

# Fallback provider (optional, disabled by default)
BINN_FALLBACK_ENABLED=false

# Redis
BINN_REDIS_URL=redis://localhost:6379/0

# Grade 7 knowledge-base PDF uploads
BINN_KNOWLEDGE_UPLOAD_DIR=var/knowledge/uploads
BINN_KNOWLEDGE_MAX_UPLOAD_BYTES=52428800
BINN_KNOWLEDGE_CHUNK_SIZE=900
BINN_KNOWLEDGE_CHUNK_OVERLAP=150
BINN_KNOWLEDGE_EMBEDDING_DIMENSIONS=768

# Persistent exercise pool and worker
BINN_EXERCISE_POOL_READY_SIZE=8
BINN_EXERCISE_POOL_REFILL_THRESHOLD=16
BINN_EXERCISE_POOL_TARGET_SIZE=24
BINN_EXERCISE_POOL_MIN_GENERATED=8
BINN_EXERCISE_WORKER_POLL_SECONDS=2
BINN_EXERCISE_WORKER_LEASE_SECONDS=900
BINN_EXERCISE_WORKER_MAX_ATTEMPTS=2

# Registration bootstrap (required only when the learners table is empty)
# Change this before creating the first learner; later registrations use a learner's own code.
BINN_BOOTSTRAP_INVITE_CODE=CHANGE-ME-FIRST
# Required only for integration/e2e learner simulations that create temporary learners.
BINN_SIMULATION_INVITE_CODE=

# Email verification. Local development logs codes from the backend in console mode.
# Public deployments must use smtp and replace the signing secret.
BINN_EMAIL_DELIVERY_MODE=console
BINN_EMAIL_VERIFICATION_SECRET=replace-with-a-long-random-secret
BINN_EMAIL_VERIFICATION_CODE_TTL_SECONDS=600
BINN_EMAIL_VERIFICATION_TOKEN_TTL_SECONDS=600
BINN_EMAIL_VERIFICATION_RESEND_SECONDS=60
BINN_EMAIL_VERIFICATION_MAX_ATTEMPTS=5
BINN_SMTP_HOST=
BINN_SMTP_PORT=587
BINN_SMTP_USERNAME=
BINN_SMTP_PASSWORD=
BINN_SMTP_FROM_ADDRESS=no-reply@binnagent.local
BINN_SMTP_STARTTLS=true
BINN_SMTP_USE_SSL=false

# Langfuse observability (optional)
BINN_LANGFUSE_ENABLED=false
BINN_LANGFUSE_PUBLIC_KEY=
BINN_LANGFUSE_SECRET_KEY=
BINN_LANGFUSE_BASE_URL=http://localhost:3100
BINN_LANGFUSE_DOCKER_BASE_URL=http://host.docker.internal:3100
BINN_LANGFUSE_ENVIRONMENT=development

# Feishu/Lark MCP group-learning source (optional)
BINN_FEISHU_MCP_ENABLED=false
BINN_FEISHU_MCP_TRANSPORT=http
BINN_FEISHU_MCP_URL=http://localhost:8765/mcp
BINN_FEISHU_MCP_DOCKER_URL=http://host.docker.internal:8765/mcp
BINN_FEISHU_APP_ID=
BINN_FEISHU_APP_SECRET=
BINN_FEISHU_OPENAPI_FALLBACK_ENABLED=true

# Debug mode
BINN_DEBUG=false

# Dev Console (debug APIs remain disabled unless explicitly enabled)
BINN_DEBUG_CONSOLE_ENABLED=false
BINN_DEBUG_CONSOLE_TOKEN=
BINN_DEBUG_CONSOLE_ALLOWED_ORIGINS=["http://localhost:5174","http://127.0.0.1:5174"]
