# 13. Current Scope and Status

> 更新时间：2026-07-10
> 目的：把当前实现、部分实现和仍处于设计中的内容显性化，避免把架构目标误读成已落地功能。

## 状态图例

| 标记 | 含义 |
|---|---|
| 已实现 | 有后端/API、前端入口或测试支撑，可运行验证 |
| 部分实现 | 已有骨架或单点能力，但学习闭环、可靠性或界面仍缺口 |
| 设计中 | 文档已有目标，代码尚未形成完整能力 |

## 当前产品线

| 产品线 | 状态 | 说明 |
|---|---|---|
| 教材线 | 部分实现 | 多教材 source 库、教材切换、split public textbook pack v2 公共种子、七年级上/下册结构化单元 workspace、单元词汇、RAG chunk、PostgreSQL 持久化任务与独立 Worker 异步补充的单元题池、双门禁六维评分、mastery-aware 选题、单元阅读语感材料生成和前端入口已存在；支持确认跳过单元并将进度记为 100%，也可重学并重置为 0%；八/九年级可上传并走通用解析/校对 fallback |
| CET 备考线 | 设计中 | 7 天计划、阅读训练、写作二改和周报仍主要在架构文档中 |
| 通用英语陪伴 | 部分实现 | Chat、Memory 摘要、Dashboard 和词汇沉淀已有基础闭环 |
| 学习者账号与邀请关系 | 基础版已实现 | 6 位邮箱验证码、短期签名令牌、重发冷却、错误次数限制、SMTP/本地日志投递、同邮箱多学习者选择、邀请码注册、直接邀请人关系、旧账号邮箱升级和空库 bootstrap 邀请码已落地；正式认证 session 尚未实现 |
| 群聊学习线索 | 第一版已实现 | 学习中心辅助入口、独立设置入口、飞书群来源配置、MCP/OpenAPI 同步、群成员拉取与当前 learner 绑定、成员映射过滤、中性 JSON 消息导入、显式标签即时抽取、`@机器人 --help` 群内操作指南回复与去重、无标签消息 pending 队列、低频 LLM 小批量线索提取、线索收件箱分页和接受/忽略/删除闭环已接入 |

## 核心路线最终收口

| 模块 | 已完成 | 未完成 | 后续计划 | 面试可讲点 |
|---|---|---|---|---|
| Learner-scoped isolation | 已新增 current user/current learner dependency、owner 校验和 scoped resource helper；Runtime、Daily Lesson、Memory、Explore、ExerciseAttempt、Debug 高风险路径已加固 | chat、knowledge、vocabulary、writing phrases、reading、dashboard 等部分旧路由仍需继续迁移 | 继续替换直接信任 `learner_id` 的路径，补覆盖剩余 router 的授权测试 | 多用户 Agent 不能只靠前端传 learner_id；学习数据、记忆、trace、推荐都必须有 ownership boundary |
| LangGraph Daily Lesson Runtime | Daily Lesson 支持 checkpoint / interrupt / resume；等待用户作答时停在 `waiting_user`，answer 后从 `grade_attempt` 恢复并闭合 grading/mastery/memory/review/recommend/verify | 当前主要是单题单 active checkpoint；生产 PostgresSaver 和官方 `interrupt()/Command(resume=...)` 深度集成未完成 | 扩展多步骤 lesson、生产 checkpointer、幂等副作用和更丰富题型 handler | 普通 Agent 一次性生成答案，学习 Agent 必须暂停等待真实用户作答；checkpoint 让学习过程可恢复、可追踪 |
| ExerciseAttempt → Mastery → Memory → Review → Recommendation | 练习提交可生成 ExerciseAttempt，MasteryEngine 更新掌握度，MemoryWriter 写学习证据，ReviewSchedule 安排复习，RecommendationEngine 推荐下一步 | 多 session 长周期掌握度、更多题型、跨模块推荐排序仍不完整 | 增加真实用户反馈闭环、错因聚合、更多 dashboard 可视化 | 个性化不是“换 prompt”，而是由作答证据驱动掌握度、记忆和复习计划 |
| Persistent Exercise Pool | 单元练习 API 已与 LLM 延迟解耦：PostgreSQL 保存 `exercise_generation_runs`，独立 Worker 通过 SKIP LOCKED、租约、有限重试和 active dedupe 补池；API 有题立即 200、空池 202，前端轮询；题目保存六维质量分并结合 mastery/覆盖约束选择 8 题 | Worker 心跳与 ownership token、失败告警、真实作答驱动的题目校准尚未完成 | 增加 Dev Console 题池观测、题目曝光/歧义/区分度指标和自动淘汰 | 异步不是简单“加队列”，而是把用户延迟、任务可靠性、内容质量和降级语义一起建模 |
| Prompt Registry + PromptExecutor + Schema-first | prompt-like model 调用已统一经 `PromptExecutor`；核心 prompt 已登记 PromptMetadata；PromptExecutor 记录 prompt hash/input hash/schema/model policy；结构化输出支持 schema validation、JSON repair、fallback decision 和 PromptExecutionRecord；chat send/stream、conversation summary、graph node/feedback、vocabulary agent、exercise generation、essay scoring、dictionary lookup、vocabulary enrichment/detail HTML 已迁移 | embedding 不属于 prompt governance；部分工具调用暂无 DB context 时只统一入口和 schema gate，不持久化 PromptExecutionRecord | 新增 prompt 必须先登记 metadata/template/schema/eval_set，再通过 PromptExecutor；继续补更多 DB context 与 regression scenario | LLM 输出不能直接写关键业务表；先验证 schema，再决定 accepted/review_required/rejected |
| Langfuse observability boundary | Langfuse 负责 raw prompt/raw output/token/cost/latency；本地 PromptExecutionRecord 只保存业务索引、schema 状态、decision 和 Langfuse reference | Langfuse dashboard 与本地 Dev Console 的跳转/聚合还可增强 | 保持本地隐私面小，补 trace id 深链和业务指标 dashboard | 不重复造 ModelCallLog；把原始观测交给 Langfuse，本地只回答“这个输出能不能写业务数据” |
| Simulation / Evaluation | `contract` / `integration` / `e2e` mode 分层；contract 可默认跑全量场景；integration 使用 deterministic fake model；impacted simulations 可按改动路径推导；baseline/threshold gate 已有 | integration 独立 test DB、真实 e2e、前端 dashboard 回归仍未完成 | contract 进默认 CI，integration 做 nightly/manual，e2e 做发布前手动回归 | Agent 应用改一处可能影响整条学习链，simulation 是防止 runtime/prompt/memory 退化的安全网 |
| ParserRun / QualityGate / ReviewQueue | 教材 ingest 已改为后台任务 + 状态轮询；新增 DocumentParseArtifact、ParserRouter、MarkItDown baseline、pypdf fallback、免费本地 OCR、文档质量指标、TextbookExtractor artifact 候选抽取；Dev Console 可看 parsing report/evidence/review queue；golden parser evaluation 已有 MVP | layout-aware extractor、多年级 golden profile、持久化任务队列未完成 | 保留为知识冷启动与质量治理，不继续扩展教材硬编码路线 | 教材解析是知识来源治理，不是产品主卖点；重点是防止低质量知识静默进入学习闭环 |
| Dev Console Debug | 已有 Learners、Recent Episodes、Graph Runs、Runtime Trace、Tool/Evidence/RAG/Prompt/Verification/Simulation/Textbook Parsing 调试入口；Debug API 默认关闭且需 token | UI polish、trace drill-down、dashboard 聚合仍可加强 | 做演示脚本、截图、关键路径可视化和部署说明 | 复杂 Agent 必须能解释“为什么这么做、用了什么工具、写了什么记忆、哪里失败了” |

## 收口判断

核心面试路线已基本完成。后续不建议继续重投入教材解析；更高收益方向是：

- 真实用户体验与 UI polish。
- 5–8 分钟稳定 demo script。
- 部署文档和一键演示数据。
- 少量关键路径 e2e 和 Dev Console dashboard。

## 共享底座状态

| 模块 | 状态 | 当前能力 | 下一步 |
|---|---|---|---|
| FastAPI API | 已实现 | learners、chat、memory、dashboard、knowledge、vocabulary、grammar 等 routers；Issue #25 第一阶段已新增 current user/current learner dependency 和 scoped resource helper，并加固 Runtime、Daily Lesson、Memory、Explore、ExerciseAttempt、Debug 高风险路径 | 剩余 routers 继续迁移到统一 current learner 认证授权 |
| React 前端 | 部分实现 | 多页面学习入口、SSE chat、知识库和词汇练习；Issue #20/#33 UI/UX 标准落地，新增统一 Button/FormField/StatusBanner/ConfirmDialog/ReasonCard/EvidencePanel；普通学习端主导航收敛为 AI对话 / 探索 / 学习中心，Header 学习者菜单承载学习设置、学习者摘要、群聊学习线索设置和登出/切换；Memory/Runtime/Trace/Prompt/Tool/Evidence 等内部页面移入独立 Dev Console；Dashboard 首页升级为学习入口页，并补学习者画像能力雷达/掌握度/薄弱点排行、学习记录热力/趋势/复习负荷图；学习设置可编辑学习目标和 CEFR 当前水平并写入 LearnerProfile；学习中心辅助入口已接入群聊学习线索收件箱和真实后端设置弹窗；词汇练习读取 learner-scoped 本地学习设置并支持跳过 setup，学习提示区可将当前词标记为“太简单（已掌握）”；KnowledgeBase 普通端收敛为今日单元 / 练习任务，支持确认跳过为 100% 和重学重置为 0%，解析校对、解析质量、教材结构内部产物和调试表格移入 Dev Console Textbook Parsing；Explore 已接入后端 ExploreCapability registry、学习能力推荐卡片和“词根与词缀”工作区；Grammar/Vocabulary/WordParts/Reading 已接入知识点配套 ExerciseBlock、练习结果摘要、AI 生成练习可编辑保存和后端优先持久化 fallback；Grammar 新增“习题巩固”工作区，可从微课 HTML 提取语法填空题 | 更深入的数据驱动推荐、恢复提示、更多页面内 drawer 化编辑 |
| LangGraph daily lesson | 部分实现 | daily lesson graph 已形成 `wait_for_answer -> grade_attempt -> update_mastery -> feedback -> memory -> review -> recommend -> verify` 单题闭环；支持可选 LangGraph checkpointer 编译和 `InMemorySaver` 测试；`LearningGraphCheckpoint` 继续持久化业务 waiting_user checkpoint；start 返回题面/schema/thread_id，answer 从 `grade_attempt` dry-run resume 并完成真实 grading、mastery、memory、review、verification；答题完成后可返回 `next_capability_recommendations` | 多步骤 lesson、生产 PostgresSaver、官方 `interrupt()/Command(resume=...)` 深度集成、更多非知识题 handler |
| Agent Runtime / Harness | 部分实现 | 新增 TaskSpec、AgentEpisode、LearningEvent、ToolCallRecord、EvidenceRef、VerificationReport、ToolRegistry、MasteryEngine、RecommendationEngine、LearningOrchestrator、LearningGraphCheckpoint；Knowledge Exercise / Daily Lesson / ExploreCapability 入口已能创建 episode trace；VerificationReport 已升级为 evidence-based deterministic/schema/business_rule/evidence checks，critical 失败会让 episode 进入 `verification_failed`，warning 进入 `completed_with_warnings`；EpisodeTraceView 返回 graph_run、checkpoint、prompt_executions、verification_report、evidence_refs 和 node_summaries；Dev Console 已提供 Learners / Model Provider / Recent Episodes / Graph Runs 上下文选择，以及 Episode Debug、Tool Registry、Tool Call Records、Evidence Debug、RAG Debug、Prompt Debug、VerificationReport、Simulation Report 和 Textbook Parsing Report；新增 `/api/debug/learners`、`/api/debug/model/provider`、`/api/debug/rag/search`、`/api/debug/graph-runs`、`/api/debug/textbook-sources`、`/api/debug/textbook-sources/{source_id}/parsing-report`、`/api/debug/textbook-sources/{source_id}/evidence`、`/api/debug/simulation/scenarios`、`/api/debug/simulation/reports/latest`；Debug API 默认关闭并要求 token | 更多工具强制走 registry、证据详情解析和可回放 UI |
| Memory | 部分实现 | 4 层学习记忆架构 + HindSight-inspired Retain/Recall/Reflect 口径已落地；统一 memory event/operation、LearningEpisode、LearnerModelMemory、TeachingStrategyMemory、MemoryWriter/Retriever/Curator、显式 L1-L4 layer metadata、ErrorPattern governance、WritingPhraseMastery；普通学习端只保留学习状态摘要，Memory Center、导出/删除/禁用/我已改善、memory_context log 和 hit-rate 指标移入 Dev Console | 更完整 debug dashboard 图表、更多 regression eval、更多 session 类型反思规则 |
| ExploreCapability | 基础版已实现 | Explore Tab 用户可见入口由 `ExploreCapabilityRegistry` 管理；`/api/explore/capabilities` 列表、`/api/explore/capabilities/{capability_id}/start` runtime 启动、learner-scoped recommendations 和 capability events 已接入；推荐只返回 ready capability，点击事件写入 Memory 并可更新偏好 | 更多能力从 not_implemented handler 走向完整闭环、更多 Memory/RAG 信号参与排序 |
| Vocabulary Learning | 部分实现 | 单元 enroll、用户可编辑个人词卡、new/review/spelling session、attempt、错因记录、mastery vector、发音 URL；“太简单”按已掌握处理并延后至少 60 天复习，到期后仍排在普通训练词之后；前端新增词根词缀库、拆词练习、localStorage 掌握标记和 morphology 展示/降级 | morphology 后端持久化、LearningProgress/Memory 联动、薄弱原因总结、题型推荐、更多表达迁移题 |
| Knowledge Base / RAG | 部分实现 | split public textbook pack v2 已物化为 KnowledgeSource、CurriculumNode、KnowledgePoint、ExerciseQuestion；overview 提供 `unit_workspace`；文档解析具备 MarkItDown/pypdf/OCR、DocumentParseArtifact、质量状态、后台 ingest、ParserRun/ReviewItem/Evidence、Golden Dataset + Parser Evaluation；单元练习采用持久化任务和独立 Worker、16/24 补池水位、立即返回/202 轮询、deterministic coverage plan、`exercise.unit_candidates`、schema/规则门禁、`exercise.unit_review` 六维评分、旧模板题归档和 mastery-aware 选题，并保留 hint/retry/rubric 反馈；另有单元阅读语感材料生成 | layout-aware extractor、hybrid retrieval、golden query set 扩展、练习 session 总结、真实作答驱动的题目质量校准、更多年级 golden profile |
| Prompt & Parsing Governance | 部分实现 | Prompt Registry MVP、PromptExecutor text/structured/stream 统一入口、`prompt_execution_records`、JSON repair/schema validation、核心业务 prompt 迁移、Debug API、`scripts/evaluate_prompts.py` 和 structured eval sets；单元题库新增独立的 `exercise.unit_candidates` / `exercise.unit_review` prompt、schema 和 eval，生成与审题均记录 PromptExecutionRecord，只有双重 accepted 结果可写入 learner-facing 题库 | 后续只允许通过 PromptExecutor 新增 prompt；补更多 PromptExecutionRecord DB context 和更完整 eval scenario |
| Model Provider | 部分实现 | Ollama chat/stream/embed/health，结构化 JSON repair retry；新增 OpenAI-compatible chat/stream provider，支持 DeepSeek 和 LongCat；`BINN_MODEL_PROVIDER` 控制部署默认 provider，Dev Console `Model Provider` 页面可 debug-only 运行时切换；PromptExecutor 会按 provider 映射 chat/utility 模型，RAG embedding 暂时隔离在 Ollama | task policy、local_only 强约束、持久化 model_call_logs、RAG embedding 云端方案 |
| Observability | 部分实现 | Langfuse observation 和运行时表 | run_id 贯通 graph/model/tool/memory，Dashboard 可视化 |
| Evaluation / Simulation | 部分实现 | `src/simulation` 提供 deterministic learner persona、behavior policy、scenario runner、assertion engine 和结构化 simulation report；runner 已分层为 `contract` / `integration` / `e2e` mode，contract 使用 MockTransport，integration 使用 ASGITransport + deterministic fake model provider，e2e 指向真实 base_url；内置 scenarios 已补 `module_tags`、entrypoints、expected events/tool calls、required metrics 和 change triggers；新增 impacted simulation 推导脚本；AssertionEngine 支持 runtime event/tool/order/absence/verification/recommendation/error/not_equals 断言；Evaluator 输出 runtime/learning/memory/recommendation/parser_rag/prompt_schema 六组 metrics；新增 baseline comparison、regressions、threshold_failures 和 gate；已覆盖 smoke、vocabulary agent deposit、vocabulary practice adaptation、episode runtime knowledge practice、daily lesson checkpoint resume/capability recommendation、missing answer no memory、verification failure blocks completed status、mastery 上下行和 LLM JSON missing field repair 场景 | 后续补更多 integration/e2e 实跑、写作好句闭环、Memory 可控性 regression、LLM-assisted learner 和 dashboard |
| CI | 已实现 | GitHub Actions 覆盖 backend lint/test、frontend lint/test/build/build:console、migration 文本检查 | Alembic 在线迁移检查和端到端 smoke |
| Writing Phrasebook | 基础版已实现 | 探索页写作入口、句式 CRUD、外部模型结果提取、候选收藏、识别/填空/替换练习和 attempt 记录 | 模型辅助编辑、精细 mastery、作文批改与翻译练习深度联动 |
| Group Learning Signals | 第一版已实现 | 新增 `group_learning_sources/participants/messages/signals` 表、来源/成员/清理/导入/分页收件箱 API；飞书群来源支持 `chat_id`、同步频率、只读/触发回复模式、sender 白名单、MCP/OpenAPI 消息同步和 `chatMembers.get` 成员同步；本地 JSON/飞书导入只接收映射到当前 learner 且开启 analysis 的文本消息；`#单词/#语法/#收藏/#怎么说/#纠错` 等显式标签即时规则抽取；`@机器人 --help` 会发送群内操作指南、记录已回复 message_id 去重且不进入学习线索候选；无标签自然消息进入 `pending_llm_analysis`，通过 `group_learning.signal_extract` 结构化 prompt 按小批量低频分析后生成候选；接受后写入 `VocabularyItem`、`WritingPhrase` 或 `LearningProgressItem` | 后续补自动调度、Memory/Mastery 权重更细化、前端导入记录列表 |

## Issue 对应落地

| GitHub issue | 本次落地 | 仍需后续 |
|---|---|---|
| #2 代码检视与优化 | 私有教材重复上传不跨 learner 复用；RAG embedding 失败记录索引质量；chat 序号约束模型对齐；CI | 统一认证授权、上传后台任务、流式 run 状态恢复 |
| #3 文档一致性 | 更新 `AGENTS.md`、`docs/web-frontend.md`、README 状态摘要和本文档 | 持续给主要架构文档补状态标签 |
| #4 MVP 缺口 | 新增当前 scope/status 表，明确 CET 与七年级教材线边界；Daily Lesson 已支持单题 answer_required checkpoint/resume | 多步骤 daily lesson、reading/writing/weekly report |
| #5 下一阶段增强 | RAG search 返回 mode/embedding/chunk/source 信息；model structured output repair retry；CI | vocabulary 多维掌握度、observability dashboard、eval dataset |
| #7 教材练习题升级 | 已移除 learner-facing 固定模板生成代码；新增单元知识点 coverage plan、结构化 LLM 候选、schema/确定性规则门禁、独立 LLM reviewer 六维评分、PostgreSQL 持久化任务、独立 Worker、租约/去重/有限重试、16/24 自动补池、200/202 API 与前端轮询、旧生成题安全归档，以及基于 learner mastery 的难度与多样性选题；保留 rubric grader、hint/retry 和答题事件 | 独立 exercise session 表、Worker 心跳与告警、结束复盘卡、更多 micro writing/roleplay 题型和真实作答反馈驱动的题库淘汰 |
| #11 词汇模块升级 | 新增 `VocabularyUserOverride`、`VocabularyMistake`、`VocabularyMasteryVector`；词汇详情和练习流读取用户覆盖层；新词学习/今日复习入口分离；用户例句优先参与填空上下文；隐藏释义不进入返回 payload；错因可修正/删除；训练中可将“太简单”标记为已掌握并以至少 60 天间隔、低于普通词的优先级重新进入训练 | 更丰富的题型生成、Dashboard 弱项聚合、roleplay/micro writing 生产题 |
| #12 好句收藏馆 | 新增 `writing_phrases`、练习和 attempt 数据表；提供 CRUD/import/exercises/attempts API；探索页接入好句收藏馆前端工作台 | P2 模型辅助编辑、P3 到期复习/mastery、P4 作文批改和翻译练习自动推荐 |
| #14 Memory Core | 新增 `learning_memory_events`、`memory_operations`、`writing_phrase_masteries`、`memory_context_logs`、`learner_memory_settings`；实现 writer/retriever/curator/explainer/manager；词汇、知识、写作句式、chat/session、作文批改、LangGraph 写入或读取统一 memory；Memory Center 已从普通学习端移入 Dev Console；支持查看 evidence、编辑、删除、禁用、我已改善、导出、手动整理、重置计划、情绪/节奏开关、低置信上下文开关；summary 增加 recent events 和 active weaknesses；metrics 增加 retrieval、hit-rate、used-in-prompt、stale | 更完整 memory debug dashboard 图表、长期 regression eval 扩展 |
| #19 HindSight-inspired Memory | 明确 Memory v2 采用 Retain / Recall / Reflect / Explain / Control；新增 `learning_episodes`、`learner_model_memories`、`teaching_strategy_memories`；`record_event()` 自动补 `evidence_ref`；`MemoryCurator.reflect()` 可从事件生成 episode、learner model、teaching strategy；`MemoryRetriever.for_chat/for_daily_plan/for_vocabulary_practice/for_knowledge_exercise/for_essay_review/for_writing_phrasebook/for_memory_explanation` 已实现；Chat、词汇、教材、作文、写作句式入口使用场景化 recall；用户删除/禁用 learner model 或 strategy 后后续 recall 排除；Memory Center 展示新卡片和指标 | 扩展更多 episode 模板、resolved 状态自动化、teaching strategy 效果评估和 dashboard 可视化 |
| #15 Learner Simulation Agent | 新增 `src/simulation/`、`tests/simulation/` 和 `scripts/run_learner_simulation.py`；至少 5 个内置 persona；deterministic scenario 覆盖 smoke journey、Vocabulary Agent 沉淀、词汇练习 adaptation、episode runtime knowledge practice 和 daily lesson checkpoint resume；runner 通过 API 调用系统并直接调用 daily lesson graph；每次运行生成结构化 report | 写作好句、Memory 可控性 regression、LLM-assisted 模式和 simulation dashboard |
| #28 Simulation / Evaluation 增强 | 已补 SimulationScenario contract、module_tags、entrypoints、expected events/tool calls、required metrics、change_triggers；新增 `scripts/list_impacted_simulations.py`；AssertionEngine 新增 event/tool/order/value/evidence/verification/recommendation/error 断言；Evaluator 新增六组 metric_groups；`SimulationReport` 新增 mode、baseline_comparison、regressions、threshold_failures；`scripts/run_learner_simulation.py` 支持 `--mode contract/integration/e2e`、`--all`、`--tag`、`--report-dir`；contract mode 可默认跑全量场景，integration mode 注入 `DeterministicFakeModelRouter`；runtime/daily lesson/vocabulary/prompt_schema scenarios 已覆盖 checkpoint resume、missing answer no memory、vocabulary adaptation、knowledge runtime practice 和 LLM JSON repair | 更多真实 DB integration/e2e 实跑、parser/RAG 专项 scenario、更多 baseline gates 和 dashboard 后续另做 |
| #16 教材解析质量止血 | 新增 `books/manifest.yaml`、parser profile、parser quality report；词汇条目增加 `raw_line`、`confidence`、`warnings` 和 `requires_review`；ingest metadata 写入 manifest/profile/report；review queue/parser evidence 由 Debug API 和 Dev Console Textbook Parsing 承载，普通学习端不展示解析治理细节 | layout-aware extractor、批量校对、校对审计历史 |
| 多年级教材上传与切换 | KnowledgeBase overview 支持 `source_id` 和 `sources[]`；上传不再拒绝八/九年级文件名，未知年级标记为 `unknown`；七下新增 manifest/profile，词表 parser 支持 `Words and Expressions` heading；未知教材至少生成“全册材料”节点和 RAG chunks | 八/九年级专用 manifest/profile、跨教材知识归并、学习进度 source 偏好持久化 |
| #17 Schema-first 回填 | 新增 `src/extraction`，写作好句导入优先 JSON schema，保留 regex fallback 且返回 `parse_mode`、`warnings`、`confidence`；新增 golden-style tests | 词汇字段回传和语法微课 machine_data 的完整保存链路 |
| #18 Prompt Registry | 新增 `src/prompts`、版本化 markdown 模板、schema/model policy 绑定、受 Debug Console token 保护的 `/api/prompts/{prompt_id}/render`；迁移 chat、vocabulary agent、grammar prompt、writing phrase prompt；新增 prompt eval fixtures | 更完整 observability 持久化、prompt evaluator、更多 P2 prompt 迁移 |
| #29 Prompt Registry + Schema-first 增强 | 新增 `PromptExecutor`、`PromptExecutionRecord`、统一 repair/validation、Debug API 和治理文档；核心 text/structured/stream prompt 已迁移；结构化输出只有 `accepted` 才能进入自动 learner-facing 链路；单元题库进一步拆分 `exercise.unit_candidates` 和 `exercise.unit_review`，分别绑定 schema、model policy、eval set 和 PromptExecutionRecord，并在两次模型调用之间加入确定性质量门禁；prompt eval CLI 与 regression tests 覆盖 schema、repair、fallback 和拒绝路径 | 后续新增 prompt 必须走 PromptExecutor，不再允许业务模块直接调用 `ModelRouter.chat()` / `stream_chat()` |
| #26 LangGraph Runtime | 新增 `build_checkpointer()`、可选 LangGraph checkpointer 编译、`wait_for_answer` interrupt-compatible 节点、`grade_attempt`、`update_mastery`、`recommend_learning_action`；Daily Lesson 恢复点改为 `grade_attempt`，start/answer 保持旧字段并新增 thread/schema/prompt 和闭环结果；Verification 支持 task_prepared、learner_answer_received、exercise_attempt_created、exercise_graded、mastery_updated、memory_event_written、review_scheduled、next_action_recommended、episode_completed、tool_calls_successful、evidence_refs_present、prompt_schema_valid；VerificationReport 增加 severity/counters/evidence refs，critical 失败阻止静默 completed；新增 `/api/runtime/episodes/{episode_id}/trace`、`/api/debug/graph-runs` 和 Dev Console Graph Runs 视图；新增 runtime/verification audit 文档和 checkpoint/missing-answer/verification-failure simulation | 生产 PostgresSaver、官方 interrupt/Command resume、更多多步骤 lesson 和 DB 副作用幂等工具化 |
| #27 教材解析质量门禁 | 新增 `ParserRun` / `parser_runs`，每次 ingest 记录 parser identity、input hash、质量报告、质量评分、产物引用和失败信息；`ParserQualityReport` 扩展 intake / structure / knowledge / vocab / RAG 指标；新增 deterministic `TextbookQualityScore`，统一 `published/review_required/partial_indexed/blocked/failed`；新增 `ParserReviewItem` / `parser_review_items`，将 low confidence、dirty token、缺页码、缺 evidence、duplicate、schema invalid、coverage gap 和 quality blocker 统一进入队列；知识点、词表、RAG chunk 写入 `parser_run_id` provenance；Review Queue API 支持查询、confirm、update、ignore 并重算门禁，pending blocker 阻止发布；Debug API 新增 textbook sources、parsing report、parser runs、review items 和 parser evidence；Dev Console 新增 `/dev/textbooks`、source detail 和 ParserRun detail；API source payload 返回 latest parser run、quality score/status、blocking reasons、pending review/blocker/warning count 和 report summary；新增 `books/golden/pep_grade7_upper`、`src/knowledge/parser_eval.py`、`scripts/evaluate_textbook_parser.py` 和 `var/parser_eval/baselines/pep_grade7_upper.json`，支持单元/词汇/语法/短语/练习 golden evaluation、baseline comparison、threshold gate 和 regression gate；新增解析审计、review queue 审计、Dev Console 审计、Parser Evaluation 审计与质量契约文档 | layout-aware extractor、批量校对审计历史、更多年级 golden profile、parser registry、后台 ingest 任务队列 |
| #30 文档解析流水线第一阶段 | 新增 `src/documents` artifact / engine / router / quality 模块；MarkItDown 作为本地上传文件 baseline engine，依赖不可用或失败时 fallback 到 PyPdfEngine；文本层弱或疑似扫描版时使用 OCRmyPDF + Tesseract 免费本地 OCR 并重新解析；后台 ingest 返回 202，`ingest-status` 暴露 stage/progress/selected_engine/attempted_engines/quality_summary；`TextbookExtractor` 改为消费 DocumentParseArtifact 并输出带 evidence 的 candidates；前端上传后展示解析中、轮询状态并提示 OCR 状态；新增 `document-parsing-pipeline.md` | 云 Document AI、layout-aware extractor、持久化 job queue、批量人工校对体验 |
| #20 UI/UX 统一标准 | 阅读 issue 正文与评论，更新 `docs/frontend-design-system.md`、`docs/web-frontend.md`、README 和本文档；新增统一 UI 原语；逐页覆盖 AppShell/Header、Chat、Explore、Dashboard、Writing Phrasebook、Grammar、Vocabulary Detail、Pronunciation、KnowledgeBase、VocabularyPractice、Dashboard 词汇工作区和 Login；学习中心升级为今日学习驾驶舱；Memory/Runtime/Debug 类页面改由 Dev Console 承载；KnowledgeBase 普通端仅保留今日单元和练习任务，解析校对/质量/结构调试集中到 Dev Console Textbook Parsing | 更深层组件拆分、更多编辑态 Drawer 化、视觉回归截图 |
| #33 用户端 UI/UX 改造 | 用户端一级导航保持 `AI对话 / 探索 / 学习中心`，`记忆` 不作为一级入口；Header 学习者菜单包含学习设置、当前学习者摘要和登出/切换；新增 learner-scoped 本地学习设置并接入词汇练习默认模式、数量、发音、setup 跳过、自动播放、拼写自动检查、答对后自动进入下一题和单元词汇范围；学习中心保持入口页定位，学习者画像补能力雷达/掌握度分布/薄弱点排行，学习记录补热力图/每日完成趋势/正确率与复习负荷趋势；文档同步普通端与 Dev Console 的 Memory 边界 | 复杂页面进一步 drawer/bottom sheet 化，更多手写按钮归一，图表数据源后续接入更完整 mastery/error aggregation |
| #23 词根与词缀 | Explore 词汇分类新增“词根与词缀”入口；新增 `WordPartsPage`，包含方法入门 / 词根词缀库 / 拆词练习 / 我的掌握四个 workspace；内置 10 个前缀、10 个词根、10 个后缀和 8 张练习卡；`VocabularyDetailPage` prompt 增加可解析构词区域要求并提供 morphology fallback；`VocabularyPracticePage` 在 new/review/spelling 中按模式展示构词提示，拼写练习只显示前后缀安全提示；新增 Vitest 数据 helper 测试 | 后端 `morphology` 字段持久化、AI HTML 构词区域解析入库、LearningProgress/Memory 沉淀 |
| #24 知识点配套练习与验收 | 新增统一前端 Exercise 类型、内置题、ExerciseBlock、ExerciseRenderer、ExerciseAttemptSummary、ExerciseLearningSignal；Grammar/Vocabulary/WordParts/Reading 场景内嵌练习块；升级现有 `exercise_attempts` 表为全局 ExerciseAttempt target 模型；为 `ExerciseQuestion` 增加统一 ExerciseItem mapper 和 `/api/learners/{learner_id}/exercises` 查询入口，课程知识库题源与内置题都具备 target/source；课程知识库练习 UI 和页面知识点验收共用 ExerciseRenderer、`ExerciseAttemptService`、summary 规则和 localStorage fallback；新增 `/api/learners/{learner_id}/exercises/generate` 与 AddExerciseForm，AI 生成题先进入可编辑表单，确认保存后作为 generated/manual ExerciseItem 进入统一渲染；语法微课支持 `grammar_fill_blank` 题型、HTML `data-exercise` 提取和“习题巩固”tab；单元阅读语感训练完成后写入 `reading_passage` attempt 作为阅读画像证据 | Memory 真正写入、mastery/error pattern 联动、后端 ExerciseItem 管理 |
| 单元阅读语感训练 | 新增 `docs/superpowers/specs/2026-07-09-unit-reading-fluency.md`、`reading.material_generation` prompt/schema/eval、`reading_material_histories` 生成上下文字段、生成材料 API 和完成阅读 API；单元学习页入口接入后进入 Reading Workshop | 阅读理解题自动评分、MemoryWriter 细粒度 Retain、Dashboard 阅读输入量可视化 |
| #32 Public textbook learning workspace | 新增 split public textbook pack v2 物化模块与迁移，公共七年级上/下册 seed 幂等写入 KnowledgeSource、CurriculumNode、KnowledgePoint、ExerciseQuestion；Knowledge Base overview 新增 `unit_workspace`，前端普通单元页改为 UnitLearningWorkspace 分组卡片，行动入口复用 daily lesson、教材练习、词汇、语法、发音和 mastery 既有流；默认学习内容只展示短结构化事实和生成检查题入口，不展示原始页文本、长篇课文、听力稿、extraction gaps 或调试表格 | 更细的单元内推荐排序、练习 session 复盘和基于真实掌握度的卡片排序 |
| #25 Learner-scoped isolation | 新增 `src/security/ownership.py`、`get_current_user()`、`require_learner_access()`、`get_current_learner()` 和 scoped resource helper；Runtime trace/verification、runtime episode list、Daily Lesson start/status/answer、Memory summary/debug actions、Explore preferences/recommendations/events/capability start、ExerciseAttempt API、Debug learners/RAG learner filter 已接入 learner owner 校验；新增 `docs/security/learner_scope_audit.md` 和隔离测试 | 将 chat、knowledge、vocabulary、vocabulary-learning、writing phrases、reading、dashboard、conversations、grammar、learning progress、sessions 等剩余 routers 迁移到统一 dependency；替换更多 source_id/node_id/session_id/item_id 直查路径 |
| ExploreCapability 推荐 | 新增 `src/explore/capabilities.py`、`src/explore/recommender.py` 和 `src/explore/schemas.py`；删除旧 Explore 入口 API；Daily Lesson answer 返回 `next_capability_recommendations`；用户点击/忽略推荐写入 capability event、Memory 和 episode trace；前端 ExplorePage 与 Daily Lesson feedback 接入推荐卡片；新增 `docs/explore-capability-recommendation.md` | 更丰富上下文排序、可配置 LLM rerank prompt、更多 capability handler 完整实现 |
| Core Runtime Upgrade | 新增 `src/runtime`、`src/evidence`、`src/mastery`、`src/recommendation`、`src/learning`、`src/verification`；Knowledge Exercise 提交写入完整 AgentEpisode trace；Daily Lesson start/answer/status 接入 TaskSpec + LearningGraphCheckpoint checkpoint/resume；ExploreCapability start 接入 TaskSpec；`/runtime/episodes/:episodeId` Debug 页面已移入 Dev Console；simulation 增加 episode runtime、checkpoint resume 和 capability recommendation regression | 多步骤 checkpoint/resume、权限隔离、更多 handler 从 not_implemented 变成完整闭环 |

## 可运行能力

```bash
./scripts/dev.sh
python -m pytest tests/ -v
cd binnagent-frontend && npm run lint && npm run test && npm run build && npm run build:console
```

## 最近风险

- 多用户远程部署前仍需把剩余 learner-scoped routers 从请求参数存在性检查迁移到统一认证依赖。
- 教材 ingest 已改为 FastAPI 后台任务，但仍不是持久化队列；进程重启时运行中任务不会自动恢复。
- Daily lesson 目前是单题单 active business checkpoint；LangGraph checkpointer 已可选编译但生产未接 PostgresSaver，后续多步骤 lesson 需要扩展官方 interrupt/resume 与幂等副作用策略。
